# Architecture

This document explains the internal design of `@safe-css/core` and the reasoning behind the choices that aren't obvious from reading the code. It assumes you've read the root [README](../README.md) for the product framing; this is the "how and why," not the "what."

## Contents

- [Styling engine](#styling-engine)
- [Theme token model](#theme-token-model)
- [Token dependencies](#token-dependencies)
- [Custom tokens](#custom-tokens)
- [Render architecture](#render-architecture)
- [Style precedence](#precedence)
- [Box](#box)
- [Stack](#stack)
- [Scoped normalization](#scoped-normalization)
- [Recipes](#recipes)
- [Scoped CSS](#scoped-css)
- [SSR strategy](#ssr-strategy)
- [Diagnostics](#diagnostics)
- [unsafeCss diagnostics](#unsafecss)
- [RTL / Overlay](#rtl--overlay)
- [Portals](#portals)
- [Future traceability ("blast radius")](#future-traceability)
- [Non-goals](#non-goals)

## Styling engine

There is no runtime CSS-in-JS engine here — no style tag injection, no class-hash generation, no `<style>` mutation on mount. Given the constraints (SSR safety, deterministic output, low runtime overhead, no large dependency), a hybrid of two much simpler mechanisms covers everything the public API needs:

1. **A single static stylesheet** (`src/style/styles.css`, shipped as `dist/styles.css`), containing every _structural_ class the primitives can produce: `display: flex`, `flex-direction`, the `align-items`/`justify-content` keyword classes, `overflow` combinations for `ScrollArea`, `position: sticky`/`position: relative`/`position: absolute`, the fixed `grid-template-columns: repeat(N, 1fr)` classes, and the Overlay anchor → `transform` lookup table. Every one of these concerns has a small, closed set of possible values (there are only 4 `align` values, only 3 `ScrollArea` directions, only 9 Overlay anchors), so they can be fully enumerated at build time. This file never changes at runtime and is identical across every app that imports safe-css.
2. **Inline `style` objects for anything token-driven.** Token names are open-ended (`createTheme` can be called with a different value for `space.card` per app, and a project can add entirely new token names via declaration merging), so there is no way to precompute a finite set of classes for them. Instead, every token-driven prop resolves to a `var(--fw-<category>-<token>)` reference written directly into the element's `style` attribute — `padding: var(--fw-space-card)`, never `padding: 16px`.

This split is the core trick: structural concerns (a closed, small vocabulary) become static CSS; value concerns (an open vocabulary controlled by the theme) become CSS variable references. Neither one needs a runtime style-generation step, a class cache, or a `<style>` tag inserted during render — which is also exactly why SSR is trivial (see [SSR strategy](#ssr-strategy)) and why there's no hydration-mismatch risk: the class list and the style object are pure functions of props, computed identically on server and client.

The alternative most comparable libraries reach for — atomic-class generation with a runtime cache (`css-in-js` engines, Tailwind-style JIT) — was deliberately avoided. It would add a real dependency, a class-name hashing scheme to keep deterministic across server/client, and a runtime cache to manage, for a public API that (by design) only ever produces a few dozen distinct structural combinations. Static CSS is simpler, smaller, and easier to audit — a strong CSS reviewer can open `styles.css` and read the entire structural vocabulary in one pass.

## Theme token model

`Theme` is a fixed interface with seven categories (`colors`, `space`, `radius`, `size`, `border`, `shadow`, `layer`), each declared as its own named interface (`ThemeColorTokens`, `ThemeSpaceTokens`, ...) rather than inlined into `Theme` directly. That's what makes `declare module "@safe-css/core" { interface ThemeSpaceTokens { xl: string } }` work — TypeScript's declaration merging unions the extra member into the existing interface, and `SpaceToken = keyof Theme["space"]` picks it up everywhere automatically.

This is a deliberate simplification versus a fully generic `createTheme<T>()` that infers a bespoke token shape per call site. A fully generic version is possible in TypeScript but requires either a global "current theme" type registered via module augmentation anyway (the same mechanism used here, just wired through a generic parameter instead of a fixed interface) or components generic over an arbitrary theme shape, which would leak into every primitive's type signature for a benefit most projects won't use. The fixed-interface-plus-declaration-merging approach gives full autocomplete for the documented default token set with zero configuration, and an explicit, documented escape valve for teams that want more — see the README's [Theme](../README.md#5-theme) section.

`createTheme(input)` deep-merges `input` over a complete built-in default theme, one category at a time (`{ ...defaultTheme.space, ...input.space }`). The result is always a fully-populated `Theme` — no component ever has to handle a partially-defined theme, which is what lets token resolution simply be "look up `theme[category][token]`" with no fallback branch.

`themeToCssVariables(theme)` is the only place a theme's _values_ ever become literal CSS strings — it flattens the theme into a `{ "--fw-space-card": "16px", ... }` map, which `ThemeProvider` applies as an inline `style` object. Every downstream primitive only ever reads `var(--fw-space-card)`, never `16px`. This is what makes a theme change safe and total: changing `theme.space.card` changes one CSS custom property, and every element referencing it (via the browser's own CSS variable resolution) updates without React needing to re-render a single one of them — though in practice a theme swap in this codebase _is_ a React re-render for the elements that actually consume the changed variable (see [Render architecture](#render-architecture) for how far that re-render actually propagates), it doesn't have to be for the visual update itself to happen, since that's the browser's own CSS engine resolving the variable.

## Token dependencies

Some tokens represent the _same_ design decision as another token, just applied differently. `border.subtle` ("what does a subtle border look like") and `colors.border` ("what is the app's border color") used to be independently-literal values in the default theme — `colors.border: "#e5e7eb"` and `border.subtle: "1px solid #e5e7eb"` — which meant changing one didn't change the other, quietly breaking the "changeable" promise for exactly the kind of edit a developer would expect to just work.

The fix is architecturally trivial _because_ the theme is already CSS-variable-backed (see [Styling engine](#styling-engine)): `border.subtle`'s value is simply written as `"1px solid var(--fw-color-border)"` instead of a repeated hex literal. Since every token becomes a CSS custom property on the same `ThemeProvider` element, and CSS custom properties resolve lazily against whatever's currently defined - not at authoring time - this "just works" with zero extra machinery:

```text
colors.border  →  --fw-color-border: #e5e7eb
                                        ↑ referenced by
border.subtle  →  --fw-border-subtle: 1px solid var(--fw-color-border)
                                        ↓ referenced by
                   Box's `border="subtle"` → style.border = var(--fw-border-subtle)
```

Change `colors.border` (e.g. the "vibrant" demo preset sets it to a purple tint) and every `border="subtle"` consumer updates automatically, without the theme needing to also override `border.subtle` - see `apps/demo/src/theme/presets.ts`, whose "vibrant" preset relies on exactly this.

This dependency model is intentionally _not_ a general expression language - there's no parser, no dependency graph, no cycle detection. It's just "a token's literal value can be a `var()` reference to another token," which the CSS engine already resolves for free. Not every pair of similar-looking tokens should be coupled this way: `border.strong` stays an independent literal (a deliberately different, more saturated shade for emphasis, not "the same decision as `colors.border`, just stronger"), and shadows (`shadow.raised`, `shadow.overlay`) stay plain `rgba(0,0,0,...)` values rather than referencing a color token, because shadow color is conventionally independent of brand color in most design systems. The goal is one source of truth per _design decision_, not maximum deduplication - see `theme/defaultTheme.ts` for the reasoning inline at each token.

This same "token references another token's CSS variable" pattern is also exactly the kind of edge a future blast-radius tool would want to walk (see [Future traceability](#future-traceability)): `colors.border` changing doesn't just affect direct consumers, it affects `border.subtle`'s consumers too, transitively.

## Custom tokens

Declaration-merging a new token name (`declare module "@safe-css/core" { interface ThemeSpaceTokens { xl: string } }`) only changes what TypeScript _accepts_ as a valid `SpaceToken` - it cannot verify that any particular `createTheme(...)` call actually supplies a value for `xl`, because by the time a consuming app's `declare module` augmentation exists, `@safe-css/core`'s own `defaultTheme` object was already compiled without any knowledge of it. TypeScript doesn't re-verify an already-compiled value against a since-augmented interface; it just trusts the ambient `const defaultTheme: Theme` declaration. This is a fundamental limitation of the declaration-merging pattern itself - shared by every library that uses it (styled-components' `DefaultTheme`, react-i18next's resource typing, etc.) - not something specific to this codebase, and there is no way to close it with types alone, because types don't exist at runtime to check against.

Given that, v0.1.1 keeps the fixed-interface-plus-declaration-merging design (rewriting it as a fully generic `createTheme<T>()` would either require the exact same module-augmentation escape valve anyway, or make every primitive generic over an arbitrary theme shape - more complexity for a benefit most projects won't use) and closes the gap two ways instead:

1. **The runtime safety net is unconditional.** `useTokenResolver` validates every token against the _actual_ theme's token names (see [Render architecture](#render-architecture)) regardless of how that token name became typeable - an augmented-but-unsupplied token produces the exact same "Unknown token" warning as a plain typo. This was already true in v0.1 for ordinary typos; v0.1.1 doesn't change the mechanism, just makes sure it's the documented, intended answer to this specific gap (see `tests/theme.test.tsx`... `tests/type-tests.ts` for the exact "type says it's fine, runtime says otherwise" scenario, reproduced and asserted against).
2. **A stricter, opt-in authoring pattern closes it further for anyone who wants it.** A full custom theme checked with `satisfies Theme` (rather than passed piecemeal to `createTheme`'s `Partial<Theme>`-typed input) _is_ checked for completeness, including any augmented tokens - `satisfies` requires every property of the target type, `Partial<...>` doesn't. See the README's [Theme](../README.md#5-theme) section for the pattern; `tests/type-tests.ts` type-tests both the completeness check and a deliberately incomplete theme failing it.

This is the honest state of the art for this pattern: full compile-time closure isn't reachable without abandoning either the shared global interface (losing "augment once, autocomplete everywhere") or the ability to build a theme incrementally via `createTheme({ ...just what changed })`. Priority order for this decision, per the product's own rules, was predictability first, then developer experience, then autocomplete, then type-system purity - which is why the answer leans on a strong, always-on runtime check rather than a more elaborate type-level one.

## Render architecture

Every primitive reads from React context once per render, via `useTokenResolver` (`src/style/useResolvedToken.ts`). What it actually needs from that context is narrow: the _set of valid token names_ per category (to validate a prop and warn if it's wrong), the diagnostics mode, and whether a `ThemeProvider` exists at all - never the theme's resolved _values_, since building a `var(--fw-space-card)` reference is pure string concatenation from a category and a token name, not something that needs `"16px"` at all.

That distinction is why the context is split into two, in `theme/ThemeContext.tsx`:

- **`ThemeMetaContext`** - `{ tokenNames, diagnostics, isProvided }` - is what `useTokenResolver` subscribes to. `tokenNames` is a `Record<category, Set<token name>>`, built by `buildTokenNameIndex(theme)`.
- **`ThemeValueContext`** - the raw `Theme` object - is published alongside it but consumed by nothing today (see [Portals](#portals) for why it exists anyway).

`ThemeProvider` computes a cheap _signature_ of the theme's token names every render (`tokenNameSignature`, e.g. `"space:none,control,element,card,section,page|..."` - a few dozen key lookups, effectively free) and only rebuilds `tokenNames` - and therefore only produces a new `ThemeMetaContext` value - when that signature actually changes. A value-only theme update (`space.card: "16px" -> "20px"`) produces the _same_ signature, so `tokenNames` stays referentially identical, `ThemeMetaContext`'s value stays referentially identical, and React's own context bailout means a descendant that only reads `ThemeMetaContext` - which is every primitive - never re-renders because of it, even though `ThemeProvider` itself did.

This is a real, verified effect, not a theoretical one: `tests/themeRenderOptimization.test.tsx` wraps a `useTokenResolver`-consuming component in `React.memo()` (with no props of its own, so `memo` bails out unless a _subscribed_ context changes) and asserts its render count doesn't move across a value-only theme swap, but does move when the token _names_ genuinely change (e.g. an augmented theme adds a new one) or when `diagnostics` changes. `React.memo` is required in the test to observe the effect in isolation - without it, a component still re-renders whenever _its own_ parent does, for the ordinary reason any non-memoized React component does; the context split only prevents re-renders that would otherwise be caused by _this specific context_ changing.

The actual CSS values never needed to be in React state to begin with - they only ever need to reach the DOM once, as the inline `style` object `ThemeProvider` computes from `themeToCssVariables(theme)` (see [Theme token model](#theme-token-model)), after which the browser's own CSS engine does the rest. Splitting the context is what stops that DOM-bound information from _also_ forcing every primitive through React's render pipeline for no reason.

## Precedence

Style precedence (`primitive defaults < recipe base < recipe variant < instance props < unsafeCss`) is implemented as an ordinary JavaScript object merge, not CSS specificity:

- Inside `defineRecipe`, the four layers are spread in order: `{ ...base, ...variantProps, ...instanceProps }`. Whatever key appears last wins. There is no CSS selector complexity involved at all — the "specificity" is just object-spread order.
- Inside each primitive, `unsafeCss` is merged last, into the already-computed `style` object: `{ ...style, ...unsafeCss }`. Because inline styles always win over class-based styles in the browser's own cascade, and `unsafeCss`'s properties are merged last within the inline style object itself, it wins over everything — both the component's own token-driven styles and (trivially) the static structural classes, which sit at the lowest possible specificity (a single class selector) by construction.

This is why the spec's constraint "no specificity games should be required" is met structurally rather than by convention: there is no scenario where two rules of equal specificity race, because there is only ever one class per structural concern applied to an element, and the token-driven properties never appear in the stylesheet at all — only in that element's own inline style.

## Box

`Box`'s prop list is intentionally asymmetric with `Stack`/`Row`/`Grid` — for example, only `Box` has `padding`, and only `Stack` has `height`. This isn't an oversight; it follows directly from "primitives expose behavior, not CSS." `Box` is a _surface_ (it has a padding box, a background, a border), so padding belongs there. `Stack` is a _layout behavior_ (arrange children vertically with a gap), and its `height`/`width` props exist only because "fill the available space" is a real, common layout need for a vertical stack — `Row`'s omission of `height` reflects that a horizontal flow's height is usually determined by its content or its parent, not something you'd set directly. When an app genuinely needs a `Row` to fill a parent's height, the answer is composition — wrap it in a `Stack` and use `grow` (see `apps/demo/src/App.tsx`'s shell for a worked example) — not a wider Box-like prop surface on every primitive "just in case." Keeping each primitive's prop list to what its own concept actually needs is what keeps the public surface small.

## Stack

See [Box](#box) above for why `Stack` has `height`/`width` but `Row` only has `width`. Structurally `Stack` is just `display: flex; flex-direction: column` plus the shared alignment/gap/sizing vocabulary described in [Styling engine](#styling-engine).

## Scoped normalization

`styles.css`'s shared selector list (`.fw-Box, .fw-Stack, .fw-Row, ...`) carries `margin: 0` alongside `box-sizing: border-box`. This exists because "parents own spacing between children via `gap`, children never set their own margin" (the whole reason there's no public margin prop at all) would otherwise have a hole: `<Box as="h1">` renders a real `<h1>`, and browsers give `<h1>`/`<p>`/etc. their own default margin via the UA stylesheet, independent of anything React or safe-css does. Without this rule, `as="h1"` would silently reintroduce exactly the "child controls its own spacing" problem the framework's core rule exists to prevent - and, before this fix, the demo had to work around it by hand: `<Box as="h1" unsafeCss={{ margin: 0 }}>`.

The fix is scoped by construction, not a global reset: it's one rule, in one file, targeting only the fixed list of `.fw-*` classes safe-css itself generates. There is no bare `h1 { margin: 0 }` or similar element selector anywhere in the file - an `<h1>` an application renders directly (not through a safe-css primitive) is completely untouched, exactly like every other rule in `styles.css` (see [Scoped CSS](#scoped-css)). `tests/marginNormalization.test.tsx` asserts both halves of this directly: that the shared rule covers every primitive class, and that no bare element selector exists in the file at all.

`unsafeCss={{ margin: ... }}` still works if an app genuinely needs to override this - it's just no longer needed for the common "a heading shouldn't carry a random default" case, and reaching for it now trips the [unsafeCss diagnostics](#unsafecss) suspicious-margin warning, which is exactly the point: overriding this default should be a visible, deliberate choice, not an accident of which tag `as` happened to render.

## Recipes

`defineRecipe`'s implementation is straightforward (merge props by precedence, `createElement` the underlying primitive), but getting its TypeScript signature right has required working around several real inference limitations. They're documented here in detail specifically so they aren't "fixed" by accident later by someone who doesn't know why the code looks the way it does.

**Why every primitive's type is two call signatures, not one.** A primitive like `Box` is polymorphic (`as="button"` changes its DOM prop set), which naturally wants a single generic call signature: `<E extends ElementType = "div">(props: BoxProps<E>) => ReactElement`. That works fine for direct JSX use. But `defineRecipe(Box, ...)` needs to _infer_ `Box`'s prop type from the value `Box` itself — and TypeScript's inference, when the source is a generic function with a default type parameter, does not reliably apply that default. The element type parameter is left unresolved, which collapses the inferred DOM-prop portion down to `any` and silently erases everything (including `onClick`) from the recipe's resulting prop type. Adding a second, concrete call signature pinned to the primitive's default tag (`(props: BoxProps<"div">) => ReactElement`) gives inference something non-generic to resolve against, which TypeScript prefers — see `src/primitives/internal/polymorphic.ts`'s `PolymorphicComponent` type and the comment there.

**How `base: { as: "button" }` gets real button DOM props (v0.1.1).** In v0.1, a recipe's exposed prop type was always pinned to its primitive's _default_ tag (`div`), even if `base.as` changed what actually rendered - `defineRecipe(Box, { base: { as: "button" } })` accepted `type`/`disabled` at runtime (they'd just get spread onto the `<button>`) but rejected them at compile time. Fixing this needed a way to ask "what are this primitive's own semantic props (`BoxOwnProps`), independent of which tag it's pinned to right now" - a question the _value_ `Box` alone can't reliably answer without reintroducing the exact inference failure above; only the _type_ alias `BoxProps<E>` can answer it cleanly, for any `E`, since it isn't inferred, it's parameterized. `PolymorphicComponent` carries a phantom marker for exactly this: `{ readonly __ownProps?: Own }` - a property that never exists at runtime (no primitive ever sets it) and whose value is never read, only its type. `defineRecipe` extracts `Own` from it (`OwnPropsOf<C>`, a conditional-type `infer`) independent of any tag, then separately infers a type parameter `E` (defaulting to `"div"`) directly from `config.base.as`'s literal value - a completely ordinary, reliable inference (matching a literal against a plain property type, nothing generic-function-shaped about it). The recipe's exposed prop type is then computed as `PolymorphicProps<E, Own>`, built by the framework's own type-level function, never inferred through a value. The result: `defineRecipe(Box, { base: { as: "button" } })` now correctly exposes `type`, `disabled`, a `MouseEventHandler<HTMLButtonElement>` `onClick`, and a `Ref<HTMLButtonElement>` - see `tests/type-tests.ts`'s `ButtonLikeRecipe` cases and `tests/recipe.test.tsx`'s runtime button-prop tests.

**Why a recipe's `variants` still aren't as strictly type-checked as its `base`.** The natural way to express "every variant's props must be a subset of the primitive's props" is a generic constraint: `V extends Record<string, Record<string, Partial<P>>>`. But when `P` (or, as of v0.1.1, `Own`) and `V` are _both_ being inferred in the same call (`Own` from the `Primitive` argument, `V` from `config.variants`), making `V`'s constraint reference `Own` causes TypeScript to widen `keyof V` to `string`, which (via `Omit<P, keyof V>`) strips every other prop from the recipe's final type — the same failure mode as the `base.as` problem above, for a related reason (two type parameters inferred from the same call, one's bound referencing the other). The fix decouples `V`'s constraint from `Own` (`V extends Record<string, Record<string, object>>`) and instead offers the primitive's-props shape only as a soft, non-constraining hint (`VariantHint<P, V>`) intersected into the parameter type — enough for editor autocomplete in the common case, but not a hard guarantee. Callers who want the guarantee back can opt in explicitly: `variants: {...} satisfies RecipeVariantMap<BoxProps>` (see `tests/type-tests.ts`'s `invalidVariantMapOptIn`, which shows the opt-in correctly rejecting an unknown prop).

`base`'s props don't have this problem — `base?: Partial<Omit<PolymorphicProps<E, Own>, "as">> & { as?: E }` only involves `Own` (already resolved by that point via `NoInfer<OwnPropsOf<C>>`) and the independently-inferred `E`, so excess-property checking works normally there. In practice this means a typo in `base` is reliably caught at compile time; a typo inside a `variants` leaf is not always caught, though React will still warn at runtime if it produces an unrecognized DOM attribute on a host element. This is the one place v0.1.1 knowingly trades a small amount of compile-time strictness for keeping the primitives' real prop types (DOM events included) intact — see the README's [v0.1.1 limitations](../README.md#16-v011-limitations).

**Recipe names are required, not optional (v0.1.1).** `defineRecipe(Box, { base: {...} })` used to accept an unnamed recipe; `name` is now a required field. An unnamed recipe is untraceable - it can't appear meaningfully in `data-fw-recipe`, and it would be a dead end for any future blast-radius tool asking "which recipes does this token change affect" (see [Future traceability](#future-traceability)). This is the one intentionally breaking API change in v0.1.1.

**Variant collisions are detected, not just silently resolved (v0.1.1).** When two different active variant groups both set the same underlying prop (e.g. a `size` variant and a `density` variant both setting `padding`), the recipe engine has always resolved this deterministically - a plain object spread in variant-group declaration order, later wins, same as every other precedence decision in this engine (see [Precedence](#precedence)) - but that resolution used to be invisible. As of v0.1.1, the `Recipe` component (inside `defineRecipe`, at render time) tracks which variant group last set each prop while building the merged style object, and if a _different_ group later overwrites the same prop with a _different_ value, it calls `warnRecipeVariantCollision` (development-only, deduplicated per unique collision, same infrastructure as every other diagnostic - see [Diagnostics](#diagnostics)). It does not warn when both groups agree on the same value, and it never changes the resolved output either way - this is purely a visibility improvement for an interaction that was always deterministic but easy to get backwards by accident.

Runtime enforcement of "no arbitrary selectors" doesn't need a runtime check at all: `variants`/`base` are always plain prop objects assigned to keys the primitive understands (or, at worst, forwarded as an inert DOM attribute) — there is no code path that accepts a selector string in the first place.

## Scoped CSS

Every rule in `styles.css` is a single class selector (a handful of Overlay RTL rules are `[dir="rtl"] .fw-anchor-*` — an attribute selector scoped to the framework's own class, still not a descendant selector reaching into arbitrary markup). There is no rule of the shape `.a .b`, no element selector (`button { ... }`), and no `!important` anywhere in the file. Combined with the precedence model above (object merge, not cascade), this is what guarantees a local instance-level prop change can never leak into a sibling, a child, or an unrelated page — the generated CSS simply has no mechanism to reach outside the element it's applied to.

## SSR strategy

Because every style decision reduces to (a) a fixed, precomputed class name and (b) a `var()` string built from plain string concatenation, rendering never touches `window`, `document`, or any browser API. `ThemeProvider` renders a normal `<div style={...}>` — inline styles are just React props, computed identically during `renderToString`/`renderToStaticMarkup` on the server and during the client's first render, so there's no hydration mismatch to guard against. `tests/ssr.test.tsx` asserts this directly by running under a plain Node Vitest environment (`// @vitest-environment node`, no jsdom at all) — if any code path referenced a browser global, that test would throw a `ReferenceError` rather than silently pass.

The one runtime environment check in the codebase (`isDevelopmentBuild()`, gating diagnostics) reads `process.env.NODE_ENV`, which is safe in both Node (where `process` is real) and in a bundled browser build (where bundlers statically replace `process.env.NODE_ENV`) — it's wrapped in a `try/catch` regardless, defaulting to "development" if the check can't be made at all, since a missed warning is worse than an extra one.

## Diagnostics

Diagnostics are additive-only: every warning is a `console.warn` call, gated by `ThemeProvider`'s `diagnostics` prop (`"warn"` by default outside production, `"off"` in production unless overridden). No diagnostic call ever changes rendered output, so flipping diagnostics on or off can never change what an app looks like — only what shows up in the console. Warnings are deduplicated per unique `(kind, key)` pair for the life of the module (`src/diagnostics/warn.ts`'s `warnOnce`), so a list of a hundred cards all using the same invalid token produces one warning, not a hundred.

Development _metadata_ (the `data-fw-*` attributes - see [Future traceability](#future-traceability)) is a separate, unconditional mechanism gated only by `isDevelopmentBuild()`, not by the `diagnostics` mode. This distinction matters: turning warnings off (`diagnostics="off"`) is something an app might reasonably do to quiet a noisy console during a migration, and doing so should not also blind a future dev-tool reading the metadata - visibility and warning-noise are two different concerns with two different on/off switches.

## unsafeCss diagnostics

`unsafeCss` is a fully-supported escape hatch (see the README's [unsafeCss](../README.md#14-unsafecss) section), which creates a real tension for diagnostics: warning on every use would be accurate but would train developers to tune the warning out entirely, defeating the point of having one (the exact "warning fatigue" failure mode that makes ignored linters useless). v0.1 warned unconditionally on any `unsafeCss` use; v0.1.1 replaces that with a small, fixed classifier (`src/diagnostics/unsafeCssHeuristics.ts`) that only flags properties matching a known risky pattern:

- **Margin properties** (`/^margin/i`) - always suspicious. This is the one category with zero legitimate exceptions in this framework's model: spacing between siblings is always the parent's job via `gap`.
- **`zIndex`** - always suspicious; `Sticky` and `Overlay.Item` both expose a `layer` token for exactly this.
- **Raw layout/positioning properties** a dedicated primitive already solves - `display`, `position`, `top`/`right`/`bottom`/`left`/`inset*`, `overflow*`, `float`, `clear`, `flexDirection`, `justifyContent`, `alignItems`, `alignContent`, `gridTemplateColumns`/`Rows`/`AutoFlow` - always suspicious, each with its own alternative named in the resulting message (e.g. `overflow` points at `ScrollArea`).
- **Arbitrary values on properties with a semantic-token equivalent** - `padding`/`gap`/`rowGap`/`columnGap`-family properties (Box/Stack/Row/Grid all expose these as space tokens) and `color`/`backgroundColor`/`background`/`borderColor`/`borderRadius`/`boxShadow`/`border` (Box exposes these as color/radius/shadow/border tokens) - suspicious _only_ when the value looks arbitrary: a raw number, or a string that isn't a recognized safe keyword (`none`, `inherit`, `initial`, `unset`, `transparent`, `currentColor`) or already a `var()` reference. `unsafeCss={{ border: "none" }}` doesn't warn; `unsafeCss={{ color: "#ff0000" }}` does.

Everything else - `cursor`, `font`, `fontWeight`, `fontSize`, `textTransform`, a decorative `transform`, and so on - never warns, regardless of value. This is a deliberately small, fixed table, not a general CSS analysis: see `apps/demo`'s own legitimate `unsafeCss` uses (badge-dot sizing, button cursor resets, typography the token system doesn't cover) for the shape of usage this is meant to leave alone.

Each warning is per-_property_, not per-`unsafeCss`-object (`unsafeCss={{ margin: 8, cursor: "pointer" }}` warns once, about `margin`, not twice and not about the whole object), reports the property and its formatted value, and states the reason plus (where relevant) the framework alternative - see the README's [unsafeCss](../README.md#14-unsafecss) section for an example message.

`unsafeCss`'s _presence_ is still always recorded, independent of whether anything warned: `mergeUnsafeCss` (`src/style/mergeUnsafeCss.ts`) returns both the merged style and a property count, which every primitive forwards into `debugAttributes` as `data-fw-unsafe-css="<count>"` (development-only, same mechanism as the rest of the traceability metadata - see [Future traceability](#future-traceability)). A `<Box unsafeCss={{ cursor: "pointer" }}>` never warns but always carries `data-fw-unsafe-css="1"` - "visible and traceable, but not noisy," per the product's own framing of this exact tension.

**Numeric value formatting.** A raw number in a CSS-in-JS style object means different things depending on the property: `padding: 13` means `13px` (how React actually applies it to the DOM), but `fontWeight: 600`, `zIndex: 5`, `opacity: 0.5`, `lineHeight: 1.4`, `flexGrow: 1`, and `order: 2` are unitless - appending `px` to any of them would misrepresent what the browser does with the value entirely, and previously did in this codebase's own diagnostic output. `src/diagnostics/cssValueFormat.ts` fixes this with a small, explicit unitless-property set (mirroring the well-known list browsers/React use internally, reproduced here since it isn't part of any public API to import) and a single `formatCssValue(prop, value)` used everywhere a CSS value appears in a diagnostic message - both the `unsafeCss` warnings above and the recipe variant-collision warning below.

## RTL / Overlay

`Overlay.Item`'s anchor system is the one place in the codebase where getting RTL right was genuinely non-trivial, and it's solved entirely in CSS, not JavaScript. The component computes each anchor's _logical_ inset properties (`inset-inline-start`/`inset-inline-end`, never `left`/`right`) directly in JS — that part is direction-agnostic by construction, since the browser resolves `inset-inline-start` to `left` or `right` based on ambient `dir` on its own. The one piece that _is_ direction-sensitive is the `transform` used for `placement="edge"` (straddling a corner/edge requires shifting the element by a percentage of its own size, and a percentage shift's sign is a physical, not logical, quantity). Rather than reading `document.dir` in JavaScript (which would mean a browser-only code path, undermining the SSR story above, and a possible flash/mismatch if `dir` isn't known synchronously), the sign flip lives in `styles.css` as a small number of `[dir="rtl"] .fw-anchor-*--edge` overrides. The component always emits the same class name regardless of direction; the browser's cascade — driven by whatever ancestor actually sets `dir="rtl"` — picks the correct transform. See `tests/overlay.test.tsx`'s RTL test, which asserts the class name is identical in both directions, and `apps/demo`'s RTL toggle for a live demonstration.

## Portals

A future Modal/Tooltip/Popover/Toast primitive will need to render outside the normal DOM tree, via `ReactDOM.createPortal`, typically into `document.body` (so the overlay isn't clipped by an `overflow: hidden`/`transform`-establishing ancestor). React context crosses a portal boundary fine - the _component tree_ still sees the portal's contents as a child, so any `useContext` call still works. CSS custom-property _inheritance_ does not: a portaled element is a DOM sibling of `document.body`'s other children, not a DOM descendant of wherever `ThemeProvider` rendered its wrapper `<div>`, so `var(--fw-color-surface)` etc. simply wouldn't resolve to anything inside a naively-portaled element.

v0.1.1 doesn't build a portal-aware primitive - that's real, non-trivial work deferred to when Modal/Tooltip/etc. actually get built - but it does keep the render-architecture change in [Render architecture](#render-architecture) from making that future work harder. The theme's raw value is still published as a second context, `ThemeValueContext`, specifically so a future portal-rendered primitive can read the _currently active_ `Theme` object and re-publish it (by rendering a nested `<ThemeProvider theme={...}>` at the portal's own root, inside `document.body`) without needing any new plumbing threaded through the rest of the tree. Nothing subscribes to `ThemeValueContext` today, so it costs the render-optimization work nothing - an unread context value never triggers a re-render in anything, by definition.

The preferred strategy when that work happens: the portal primitive reads `ThemeValueContext`, renders `createPortal(<ThemeProvider theme={theme}>{content}</ThemeProvider>, document.body)`, and gets full CSS variable inheritance back for free inside the portaled subtree, using the exact same `ThemeProvider` every other primitive already relies on - no new theme-propagation mechanism, no CSS variable duplication scheme, no `getComputedStyle` reads.

## Future traceability

A later "blast radius" feature (§32 of the original spec: "changing `radius.control` affects these N components across these M screens") needs a data source connecting theme tokens → primitives → recipes → variants → rendered instances. v0.1 doesn't build the analysis or the UI, but it does generate exactly that data source as a side effect of normal rendering, at effectively no cost:

- Every primitive collects the list of tokens it actually resolved (`space.card`, `radius.card`, `color.surface`, ...) while computing its style, and — in development builds only — writes them to a `data-fw-tokens` attribute.
- Every primitive writes `data-fw-primitive="Box"` (etc.).
- Every recipe instance additionally writes `data-fw-recipe="Card"` (always - `name` is required as of v0.1.1) and `data-fw-variant="tone:raised"` when a variant is active.
- Every primitive that received `unsafeCss` writes `data-fw-unsafe-css="<count>"` (v0.1.1) - see [unsafeCss diagnostics](#unsafecss).

All of this is centralized in one place, `src/primitives/internal/debugAttributes.ts` - every primitive calls the same `debugAttributes({...})` function right before render rather than each reimplementing its own subset of this logic, so the metadata model (which attributes exist, when they're included) has exactly one definition to change.

None of this exists in production output (`isDevelopmentBuild()` gates it, same as diagnostics), so it costs nothing at runtime for real users. But it means a future tool doesn't need a redesign of the rendering pipeline to exist — it can be built as a dev-mode DOM walker (or a browser extension, or a Storybook addon) that reads these attributes directly, correlates them against a theme's token table (including the [token dependencies](#token-dependencies) between tokens themselves), and answers "what does changing `radius.control` touch" without the framework's core architecture changing at all.

## Non-goals

Deliberately out of scope for v0.1 (and not accidentally missing): a button/input/select/modal/tabs/tooltip component library, a forms or data-table system, an animation framework, an icon system, Tailwind compatibility, non-React targets, a visual builder, an AI generator, a browser extension, a full CSS debugger, or a production-ready blast-radius UI. safe-css solves the layout/theming/scoping problem; it does not try to be a full design system or hide CSS from developers who already know it.
